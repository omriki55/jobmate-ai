"""Conversation state constants for the onboarding ConversationHandler."""

(
    AWAITING_CV,
    AWAITING_ROLE,
    AWAITING_LOCATION,
    AWAITING_SALARY,
    AWAITING_INDUSTRY,
    AWAITING_COMPANY_SIZE,
    AWAITING_EMPLOYMENT_TYPE,
    AWAITING_CONFIRMATION,
) = range(8)
